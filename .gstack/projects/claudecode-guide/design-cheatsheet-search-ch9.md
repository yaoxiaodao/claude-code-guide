# Design: Cheat Sheet + Search + Chapter 9 Enhancement

Generated by /office-hours
Status: APPROVED

## 变更范围

### 1. Cheat Sheet 快速参考页

**位置：** `docs/guide/cheatsheet.md`
**导航：** nav + 首页底部

**内容结构：**
- 斜杠命令速查表
- 常用工作流 prompt 模板
- 快捷键清单

### 2. Local Search 中文搜索

**配置：** `docs/.vitepress/config.ts`
**插件：** `vitepress-plugin-search` + 中文分词

### 3. 第九章：代码审查补全

**目标：** ~500 字，实用 prompt 模板 + 深度结合
**文件：** `docs/guide/3-practical-workflows/09-code-review.md`

## 技术方案

### Search 配置
```ts
import { localSearch } from 'vitepress-plugin-search'

export default defineConfig({
  plugins: [
    localSearch({
      tokenize: 'jieba',  // 中文分词
      // ...其他配置
    })
  ]
})
```

### Cheat Sheet 内容
- `/review` 代码审查
- `/plan` 制定计划
- `/compact` 压缩上下文
- `/ask` 快速提问
- `/rewind` 回退
- `/diff` 查看变更

### Chapter 9 补全方向
- 为什么 Claude 擅长代码审查
- 实际审查 prompt 模板（3-5个）
- 按类型分类：安全、性能、可读性、逻辑
- 配合 `/review` 命令使用

## 实施顺序

1. Cheat Sheet 页面创建
2. Nav + 首页添加入口
3. Search 插件配置
4. 第九章内容补全
5. 构建验证
