{
  "name": "claudecode-guide",
  "version": "1.0.0",
  "type": "module",
  "description": "Claude Code 中文渐进式教程",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "push": "git add -A && git commit -m 'chore: update' && git push github master && git push origin master"
  },
  "devDependencies": {
    "vitepress": "^1.5.0",
    "vue": "^3.5.0"
  }
}